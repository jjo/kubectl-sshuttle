version: 2

builds:
  - binary: kubectl-sshuttle
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - formats:
      - tar.gz
    files:
      - LICENSE

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
